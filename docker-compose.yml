version: "3.2"
services:
  api:
    build: .
    ports:
      - "$API_HOST_PORT:8081"
    environment:
      - EGO_DB_HOST
      - EGO_DB_PORT
      - EGO_DB
      - EGO_DB_USER
      - EGO_DB_PASS
      - EGO_SERVER_GOOGLE_CLIENT_IDS